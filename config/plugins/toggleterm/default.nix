{
  # toggleterm

  plugins = {
    sniprun = {
      enable = true;
      settings = {
        live_mode_toggle = "on";
        display = [ "Terminal" "VirtualText" ];
      };
    };
    toggleterm = {
      enable = true;

    };
  };

}
